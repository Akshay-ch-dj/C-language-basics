/*Purpouse: next stage to memorize all with variables and data types
Date: 03-08-19 10:40 PM */
#include <stdio.h>

int main()
{
    int aks=2, shn=3, umb=1;
    char str[190];
    printf("Enter some fk value\n");

    scanf("%d", &umb);
    shn=5;
    printf("Enter some name:\n");
    scanf("%s", str);

    printf("The matches are %d::::%d<><><><>%d{}{}{}{}{}%s\n", aks, shn, umb, str);
    /*%d is used to print(show) integers %s used to show strings=thats representing the data
    types we need to display followed by the name of the variables want to display*/
    return 0;
}

